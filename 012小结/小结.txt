

将前面实现过的一些功能通过ui界面，逐一实现

机器上电，若是首次，则需要校准，校准数据存放在nand里某个位置，新建一个结构体来存放，如果读取nand发现结构体里flag位有值，那么不需要jiaozhun

第二步是画ui交互界面mainpage();，画两行圆形，圆内写上对应功能的提示，这时候要读取屏幕按下的数据，即lcd_x,lcd_y，判断是在哪个圆内

第三步根据按下的lcd坐标返回值，在switch里选择不同功能，这里的每个功能为xxpage，比如ledpage();，不同功能会进入不同的页，

第四步，这些页应该有一个return按键，读取触屏坐标，一旦按下，返回mainpage();



校准完毕后进入功能菜单


	led		按键中断		lcd

	ts画线		i2c		校准	


